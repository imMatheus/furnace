{
	"name": "furnace",
	"version": "0.1.0",
	"description": "A firestore ODM for secure typescript usage",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"watch": "tsc -w",
		"test": "jest",
		"lint": "prettier --check --plugin-search-dir=. . && eslint --ignore-path .gitignore .",
		"format": "prettier --write --plugin-search-dir=. ."
	},
	"keywords": [
		"firebase",
		"odm",
		"orm",
		"typescript"
	],
	"author": "Matheus Mendes",
	"license": "MIT",
	"devDependencies": {
		"@types/jest": "^27.5.1",
		"jest": "^28.1.0",
		"nodemon": "^2.0.16",
		"prettier": "^2.6.2",
		"typescript": "^4.6.4"
	}
}
